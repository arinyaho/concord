{
  "name": "agent-team",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal single-tenant multi-agent dev-team control plane (headless slice).",
  "scripts": {
    "test": "node --test",
    "smoke": "node smoke/auth-smoke.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.205",
    "discord.js": "^14.26.5"
  }
}
